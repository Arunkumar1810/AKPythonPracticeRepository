coordinates = (4, 5, 10)
print(coordinates[1])  # tuples are indexed starting from zero
# coordinates[1] = 10 this will throw an error because tuples are immutable

# tuples are like list but immutable final lists are tuples

